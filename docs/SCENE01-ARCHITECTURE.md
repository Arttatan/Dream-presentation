# Экран 01 — архитектура (v2)

## В чём была проблема

1. **Рисовал сцену кодом** (луна, звёзды, градиенты) — не совпадало с макетом.
2. **Раньше вставлял скриншот с текстом** — двойной текст, размытие.
3. **Нужно:** готовый **арт без текста** + HTML-слои сверху.

## Структура

```
Scene01
├── BackgroundImage   — /scenes/scene01.png, cover, статично
├── Overlay           — лёгкое затемнение слева
├── Content           — заголовок, кнопки, подпись (z-50)
└── Navigation        — 01, прогресс, меню, подсказка (z-60)
```

## Замена арта

Положите **арт без текста** (мин. 1920×1080):

`public/scenes/scene01.png`

Перезагрузите страницу (Ctrl+F5).
